# Progress Report: Colab Bug Fixes and Inpainting Correction
**Date:** 2026-03-13
**Session:** 02

## What was done
- Fixed three bugs found during live Colab testing
- Centralized dependency management to use requirements-colab.txt across all notebooks
- Corrected the scientific description of SCIGEN's constraint mechanism

## Files changed
- `requirements-colab.txt` — added mp-api, chgnet, scipy; updated header comments
- `notebooks/00_setup.ipynb` — fixed `total_mem` AttributeError (PyTorch 2.10 compatibility), fixed `pymatgen.__version__` AttributeError, install cell now uses requirements-colab.txt
- `notebooks/01_crystal_structures.ipynb` — setup cell uses requirements-colab.txt
- `notebooks/02_materials_project.ipynb` — setup cell uses requirements-colab.txt
- `notebooks/03_generative_concepts.ipynb` — corrected constraint description from "fixing positions throughout denoising" to "inpainting approach"; updated visualization titles
- `notebooks/04_mlip_evaluation.ipynb` — setup cell uses requirements-colab.txt
- `notebooks/05_scigen_generation.ipynb` — setup cell uses requirements-colab.txt with PyG wheel install; corrected inpainting description in Section 5.1

## Decisions made
- All notebooks install from `requirements-colab.txt` (single source of truth for dependencies)
- Removed GPU memory display from setup (fragile across PyTorch versions, not essential info)
- Used `importlib.metadata.version()` for pymatgen version check (works across all versions)
- SCIGEN constraint mechanism described as "inpainting" — model generates all atoms, constrained positions are revealed at the end

## Open issues / next steps
- Continue testing notebooks 01–05 in Colab
- tutorial_colab.ipynb still uses inline pip installs (could be updated for consistency)
