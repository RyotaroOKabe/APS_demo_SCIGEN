# Progress Report: Improve PyG wheel matching and install UX
**Date:** 2026-03-12
**Session:** 04

## What was done
- Fixed PyG wheel URL to truncate PyTorch version to major.minor.0 (e.g., 2.5.1 → 2.5.0), which better matches the available prebuilt wheels on `data.pyg.org`
- Added `%%time` magic to the install cell so users see actual elapsed time
- Added markdown note setting expectations (~1-2 min with wheels, ~10 min if compiled from source)
- Moved wheel URL construction to a Python variable passed via env var for cleaner shell expansion

## Files changed
- `notebooks/tutorial_colab.ipynb` — cells 3-5: improved version detection, wheel URL, install timing, and user guidance

## Decisions made
- Cannot skip torch-scatter/torch-sparse/torch-cluster — they're imported in 6 core SCIGEN modules
- Truncating to x.y.0 is the standard approach since PyG wheel index typically only publishes for .0 patch versions

## Open issues / next steps
- If Colab upgrades to a very new PyTorch version not yet on the PyG wheel index, compilation from source will still be required — no workaround for that
- Could explore caching built wheels to Google Drive, but adds complexity
