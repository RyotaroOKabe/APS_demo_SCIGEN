# Progress Report: Fix SCIGEN DFT download in NB04
**Date:** 2026-03-14
**Session:** 03

## What was done
- Fixed Figshare download of SCIGEN DFT-relaxed structures in NB04
- Root cause: `figshare.com/ndownloader/` URLs return AWS WAF challenge (HTTP 202, 0 bytes) — blocks all non-browser downloads
- Solution: Use `api.figshare.com/v2/file/download/57245942` which bypasses WAF and returns a proper redirect to S3
- Updated archive format from `.tar.gz` to `.zip` to match actual Figshare file
- Updated directory name from `03_scigen_materials_dft` to `03_scigen_materials_relaxed` to match zip contents

## Files changed
- `notebooks/04_scigen_generation.ipynb` — cell `scigen_download_code`: changed download URL, archive format (tarfile → zipfile), and target directory name

## Decisions made
- Used Figshare API endpoint instead of ndownloader to avoid WAF bot detection
- Matched directory name to actual zip contents (`03_scigen_materials_relaxed`)

## Open issues / next steps
- Verify the notebook runs end-to-end on Colab with the new download URL
