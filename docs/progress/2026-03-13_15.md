# Progress Report: Fix notebook 03 cell types, add DDPM equations
**Date:** 2026-03-13
**Session:** 15

## What was done
- Fixed multiple code-as-markdown cells in notebook 03 (03_generative_concepts.ipynb):
  - Reverse denoising code (`eoalw3cmjfs`) was markdown → converted to explanation markdown + added proper code cell
  - Inpainting visualization (`cell-13`) was markdown → changed to code cell
  - Deleted 3 duplicate cells (two duplicate inpainting vizs, one duplicate key takeaways)
- Added comprehensive DDPM equations to section 3.3:
  - Forward process: $q(x_t | x_{t-1})$ with noise schedule $\beta_t$
  - Reparameterization trick: direct jump to step $t$ via $\bar{\alpha}_t$
  - Reverse process: learned mean $\mu_\theta$ and noise prediction $\epsilon_\theta$
  - Training loss: simple MSE on noise prediction
  - Score function relation: $s_\theta = -\epsilon_\theta / \sqrt{1-\bar{\alpha}_t}$
  - SCIGEN-specific: joint diffusion on coordinates, types, and lattice
- Added markdown explanations before each code block:
  - Forward process intro (3.4 subsection)
  - Reverse process explanation with score function approximation equation
  - Noise schedule explanation with $\bar{\alpha}_t$ definition
- Enhanced section 3.5 with inpainting math ($x = (x^{known}, x^{unknown})$)
- Updated key takeaways with equations
- Restored SC table code cell and exercise code cell that were lost during cleanup

## Files changed
- `notebooks/03_generative_concepts.ipynb` — major restructuring: fix cell types, add equations, delete duplicates, add explanatory markdown

## Decisions made
- Used standard DDPM notation ($\beta_t$, $\alpha_t$, $\bar{\alpha}_t$, $\epsilon_\theta$) consistent with Ho et al. 2020
- Kept physics analogies alongside equations (heating/crystallization metaphor)
- Added Langevin dynamics equation for the toy denoising step explanation

## Open issues / next steps
- None
