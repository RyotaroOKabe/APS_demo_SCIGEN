# Progress Report: Interactive PyVista viewer + PythTB band structures
**Date:** 2026-03-13
**Session:** 14

## What was done

### 1. Interactive PyVista viewer (`crystal_viz.py`)
- Added `plot_crystal_interactive()` function to `scripts/crystal_viz.py`
- Uses ipywidgets sliders (atom scale, elevation, azimuth, zoom) and a supercell dropdown
- Re-renders the PyVista static image on each slider change (avoids unreliable trame backend)
- Falls back to static `plot_crystal()` if ipywidgets is unavailable
- Updated notebook 01 (cells `43u4pa3kowi`, `36yssqi0n7s`) and notebook 05 (cell `1etps9hq1su`) to use the interactive viewer

### 2. PythTB tight-binding band structures (notebook 01)
- Added 6 new cells after the kagome frustration discussion (cell-26):
  - Markdown intro: geometry → electronic structure connection
  - Code: kagome vs honeycomb vs triangular band structures side-by-side (flat band, Dirac cone, cosine band)
  - Markdown: kagome + interstitial atom explanation
  - Code: kagome + interstitial with varying t' (0, 0.2, 0.5, 1.0) showing flat band destruction
  - Markdown: hopping strength / atom type effect
  - Code: kagome with Mn-like, Fe-like, Co-like, Ru-like hopping strengths showing flat band robustness

### 3. Cleanup
- Deleted duplicate `cell-16` in notebook 01 (was duplicate of `yrhkbmnufi`)
- Added `pythtb` to `requirements-colab.txt`

## Files changed
- `scripts/crystal_viz.py` — added `plot_crystal_interactive()` function
- `notebooks/01_crystal_structures.ipynb` — interactive PyVista, PythTB band structure section, deleted duplicate cell
- `notebooks/05_scigen_generation.ipynb` — interactive PyVista viewer for generated structures
- `requirements-colab.txt` — added pythtb dependency

## Decisions made
- Used ipywidgets + static re-render approach for interactive PyVista (most reliable in Colab)
- Placed PythTB section after kagome lattice discussion (section 1.5) to motivate WHY these lattice geometries matter electronically
- Used three scenarios: (i) pure lattice comparison, (ii) kagome + interstitial coupling, (iii) varying hopping strength / atom type

## Open issues / next steps
- The PythTB interstitial coupling hoppings assume specific neighbor geometry; may need validation
- Could add interactive hopping parameter slider with ipywidgets
