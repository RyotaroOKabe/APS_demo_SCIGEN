# Progress Report: Add DiffCSP notebook (04), create diffcsp package, renumber to 6 notebooks
**Date:** 2026-03-14
**Session:** 02

## What was done

### New notebook: 04_diffcsp.ipynb (19 cells)
- **4.1** DiffCSP vs SCIGEN: architecture overview, three diffusion components
- **4.2** Load pretrained model (reuses SCIGEN checkpoint)
- **4.3** Forward process: noise schedules (cosine beta for lattice, exponential sigma for coords), wrapped normal for fractional coordinates
- **4.4** PyVista rendering of noisy structures at different timesteps (t=0 to t=999)
- **4.5** Reverse process: predictor-corrector sampling, generate structures in vanilla (unconstrained) mode
- **4.6** Reverse trajectory visualization with PyVista filmstrip
- **4.7** Training overview: loss function with wrapped normal score, early termination note
- **4.8** DiffCSP vs SCIGEN comparison table

### New package: diffcsp/
- Minimal package that reuses SCIGEN's shared code:
  - `diffcsp/common/` → re-exports from `scigen.common` (identical files)
  - `diffcsp/pl_modules/diff_utils.py` → re-exports from `scigen.pl_modules.diff_utils`
  - `diffcsp/pl_modules/diffusion.py` → copied from DiffCSP (CSP-only model, not in SCIGEN)
  - `diffcsp/pl_modules/diffusion_w_type.py` → copied from DiffCSP (differs from SCIGEN version)
  - `diffcsp/pl_modules/cspnet.py`, `gnn.py`, `model.py` → copied from DiffCSP (differ slightly)
  - `diffcsp/pl_data/` → re-exports from `scigen.pl_data`

### Renumbered notebooks
- `04_scigen_generation.ipynb` → `05_scigen_generation.ipynb` (section numbers 4.x → 5.x)
- `03_mlip_evaluation.ipynb` → updated "What's next" to point to NB04 (DiffCSP)
- README updated from 5 to 6 notebooks

## Files changed
- `notebooks/04_diffcsp.ipynb` — new (19 cells)
- `notebooks/05_scigen_generation.ipynb` — renamed from 04, section numbers updated
- `notebooks/03_mlip_evaluation.ipynb` — cross-references updated
- `diffcsp/` — new package (10 files)
- `README.md` — updated notebook table

## Decisions made
- Reuse SCIGEN's pretrained model for DiffCSP tutorial (same architecture, just run in vanilla mode)
- Minimal diffcsp package: re-export shared code from scigen, only copy files that differ
- DiffCSP notebook focuses on understanding the diffusion process itself, while NB05 focuses on constraints
