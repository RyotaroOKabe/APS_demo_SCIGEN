# Progress Report: Fix NameError bugs in notebooks 01 and 04
**Date:** 2026-03-13
**Session:** 09

## What was done
- Fixed `NameError: name 'kagome_struct' is not defined` in notebook 01 by adding a guard that recreates the structure if the previous cell was skipped
- Fixed `NameError: name 'plt' is not defined` in notebook 04 by reordering cells: moved batch evaluation code before the speed comparison and parity plot visualizations, and added `import matplotlib.pyplot as plt` to the batch eval cell
- Restored the speed comparison bar chart and parity plot cells that were previously removed from the working copy, now placed in correct execution order

## Files changed
- `notebooks/01_crystal_structures.ipynb` — Cell 24 (kagome visualization) now includes a fallback that recreates `kagome_struct` if not already defined
- `notebooks/04_mlip_evaluation.ipynb` — Replaced duplicate/misplaced batch eval cell with correctly ordered cells: batch eval (with plt import) -> speed comparison markdown -> speed comparison bar chart -> parity plot -> section 4.6

## Decisions made
- Used `if 'kagome_struct' not in dir()` guard pattern for robustness rather than merging cells, keeping the notebook structure clean
- Added `import matplotlib.pyplot as plt` to the batch eval cell (cell 14) so plt is guaranteed available for subsequent visualization cells

## Open issues / next steps
- The interactive browser cell in notebook 01 (cell 13, `ys2ot96zu2p`) uses `plt` and `plot_structure` before they are defined in cell 15; this is wrapped in try/except but would fail silently
