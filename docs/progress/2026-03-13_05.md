# Progress Report: Enhance notebook 03_generative_concepts with new cells and colorblind-safe palette
**Date:** 2026-03-13
**Session:** 05

## What was done
- Embedded lattice type illustration image in section 3.7 (before the sc_table code cell)
- Added noise schedule visualization (beta_t and alpha_bar_t plots) after the forward process visualization
- Added animated forward/reverse diffusion GIF using matplotlib FuncAnimation after the reverse process visualization
- Added exercise section with prompts and empty code cell before "Key takeaways"
- Updated all `c='coral'` scatter plot colors to `c='#E69F00'` (colorblind-friendly orange-gold) in the reverse process cell, noise schedule cell, and animation cell

## Files changed
- `notebooks/03_generative_concepts.ipynb` -- Added 7 new cells (3 markdown, 3 code, 1 exercise code stub) and updated colors in the reverse process code cell

## Decisions made
- Used `#E69F00` consistently across all new and modified cells where `coral` was previously used or specified
- Noise schedule plot uses `steelblue` for beta_t (consistent with forward process) and `#E69F00` for alpha_bar_t (consistent with reverse/denoising theme)

## Open issues / next steps
- Verify the image path `../assets/SI_arch_lattice_unit_bk.png` exists and renders correctly
- Test the animation cell in Colab (FuncAnimation + to_jshtml can be slow on free tier)
