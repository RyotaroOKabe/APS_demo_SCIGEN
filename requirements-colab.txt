# SCIGEN Colab Tutorial — Dependencies for all tutorial notebooks (00–05)
#
# PyTorch + CUDA are provided by the Colab runtime.
# torch-scatter, torch-sparse, torch-cluster must be installed separately
# from the PyG wheel index matching your torch+CUDA version — see setup cell.

# Core SCIGEN dependencies (generation)
torch-geometric>=2.3.0
lightning>=2.0.0
hydra-core>=1.2.0,<1.4
omegaconf>=2.1.0
python-dotenv
pymatgen>=2023.9.0
smact
einops
p-tqdm
pathos
pyxtal

# Tutorial extras (notebooks 02, 04)
mp-api
chgnet
scipy

# Visualization (3D crystal rendering, plots, animations)
matplotlib
pyvista
Pillow
